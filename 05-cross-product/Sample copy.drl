package com.sample

import com.sample.Customer;
import com.sample.Product;
import com.sample.Order;


rule FindHighPriceOrder
    when
        $c : Customer(extraDiscount > 1000)
        $p : Product()
        
        eval ($p.price > 500)
        $o : Order(price > $p.basePrice - $c.extraDiscount)
    then
        //...
end
